/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'david-nguyen.vercel.app', 'media.licdn.com', 'imgur.com']
    }
}

module.exports = nextConfig
